version: 2.1

jobs:
  print-environment:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run:
         name: "display variable"
         command: echo what I want in the console

workflows:
  displayworkflow:
    jobs:
      - print-environment
